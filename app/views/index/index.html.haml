%h1 XueTang Project!!
%h4 fear me!!! 2333333
%br
= "[sid = #{@currentSID}], username = #{@user.name}"
%table
  %thead
    %tr
      -@top_categories.each do |category|
        %td= link_to "#{category.name}", category_path(category)
